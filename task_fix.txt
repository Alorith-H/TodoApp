请修改 C:/Users/28351/Desktop/TodoApp 这个 Flutter 项目，解决以下问题：

## 问题 1：分类标签字体颜色白色看不清
主界面每个任务卡片底部会显示一个小标签（如 "💼 工作"），它的背景色有颜色而文字也是该颜色的深色版本。在浅色主题下文字看不见。
**要求：** 文字颜色固定使用 `Colors.black87`（浅色主题）或 `Colors.white70`（深色主题），不随标签颜色变化。

## 问题 2：分类筛选 Chip 不需要图标
主界面顶部的分类筛选栏（"全部"、"工作"、"个人"等 Chip）里，当前显示的是文字。但任务卡片底部的分类标签现在显示的是 "💼 工作" 这样的 emoji+文字。
**要求：** 任务卡片底部的分类标签 **只显示文字**（"工作"），不要显示 emoji 图标。保持"标签"按钮旁边的图标。

## 问题 3：添加任务菜单里也要有"新增标签"功能
当前 "新建任务" 和 "编辑任务" 页面的分类选择器用的是 ChoiceChip 列表，但无法在这里新增自定义标签。
**要求：** 在分类选择器区域最后面加一个 "+" 按钮（或 ActionChip），点击后弹出和主界面一样的"添加标签"对话框（输入名称、选择图标、选择颜色）。添加成功后分类列表自动刷新。

## 问题 4：添加任务页面分类选择器的字体颜色
和问题 1 相同——ChoiceChip 里的文字颜色在浅色主题下可能看不清。
**要求：** 确保 ChoiceChip 文字颜色固定为黑/白色，不跟选中颜色走。

## 代码位置
- 主界面分类标签：`lib/screens/home_screen.dart` 中的 `_buildCategoryLabel()` 方法
- 主界面添加标签对话框：同上文件的 `_showAddCategoryDialog()` 方法
- 任务表单分类选择器：`lib/screens/task_form_screen.dart` 中的 `_buildCategorySelector()` 方法
- 数据库已支持 addCategory：`lib/database/database_helper.dart` 中的 `addCategory()` 方法

请直接修改代码，不要询问审批。
